## Findings

### `jekyll_mermaid_prebuild/digest_calculator_spec.rb`

- **Location:** `jekyll_mermaid_prebuild/digest_calculator_spec.rb:8` — `computes 8-character MD5 digest`
  - **Smell:** `naming-lies`
  - **Rationale:** The title claims an MD5-specific guarantee, but the body only verifies generic output shape (string, length 8, lowercase hex) and never checks the known MD5 result for a fixed input. This matches a title-body mismatch signal for `naming-lies`. See <https://texarkanine.github.io/slobac/taxonomy/naming-lies/>.
  - **Prescribed remediation:** Either rename to a behavior it actually verifies (for example "returns an 8-character lowercase hex digest") or strengthen the assertion to validate MD5-specific output for fixed fixtures.
  - **Why this isn't a false positive:** This is not an under-specified but truthful title; the current wording explicitly promises a specific algorithm contract that the assertions do not test.

- **Location:** `jekyll_mermaid_prebuild/digest_calculator_spec.rb:38` — `returns a valid digest`
  - **Smell:** `vacuous-assertion`
  - **Rationale:** The test asserts only type and length for empty input and omits even a hex-format check or expected value, so many wrong outputs still pass. This matches the weak-oracle signal where structural checks are the only assertions. See <https://texarkanine.github.io/slobac/taxonomy/vacuous-assertion/>.
  - **Prescribed remediation:** Strengthen with the strongest available oracle, such as exact expected digest for empty string or at minimum a strict hex-format assertion plus a deterministic value check.
  - **Why this isn't a false positive:** The SUT contract here is a positive return value, not side-effect absence or type-narrowing scaffolding, so weak structural-only checks are genuinely vacuous.

### `jekyll_mermaid_prebuild/generator_spec.rb`

- **Location:** `jekyll_mermaid_prebuild/generator_spec.rb:318` — `generates figure with linked image`
  - **Smell:** `presentation-coupled`
  - **Rationale:** The test verifies behavior through a chain of HTML substring checks against rendered output instead of asserting parsed structure, making it brittle to cosmetic formatting changes. This matches the rendered-string coupling signal. See <https://texarkanine.github.io/slobac/taxonomy/presentation-coupled/>.
  - **Prescribed remediation:** Parse the fragment and assert semantic structure (figure exists, anchor href matches URL, image src and alt attributes are present) rather than exact string fragments.
  - **Why this isn't a false positive:** Rendering fidelity is not the explicit product contract under test; the behavior is structural HTML semantics, so raw-string coupling is incidental and fragile.

- **Location:** `jekyll_mermaid_prebuild/generator_spec.rb:328` — `emits two links and prefers-color-scheme CSS when dark_url is set`
  - **Smell:** `presentation-coupled`
  - **Rationale:** The test hard-codes multiple CSS and HTML string snippets and class fragments, which can fail under harmless formatting or ordering changes while preserving semantic output. This matches the long rendered-output assertion pattern. See <https://texarkanine.github.io/slobac/taxonomy/presentation-coupled/>.
  - **Prescribed remediation:** Parse and assert semantic invariants: presence of light and dark anchors, correct href/src mappings, and existence of a dark-mode media rule independent of exact formatting.
  - **Why this isn't a false positive:** This is not a formatter or renderer byte-exact contract test; the expected guarantee is content structure and linkage, not exact serialized presentation.

## Behavior Summaries

| File | Line | Test ID | Behavior | Tier | Smells Found |
|------|------|---------|----------|------|--------------|
| jekyll_mermaid_prebuild/digest_calculator_spec.rb | 8 | computes 8-character MD5 digest | Verifies that `DigestCalculator.content_digest` returns an 8-character lowercase hex-like token for a two-line mermaid graph input; SUT entry point is `.content_digest(content)`; assertions target Ruby String type, fixed length 8, and regex format `/^[0-9a-f]{8}$/`; fixture shape is a minimal `graph TD` diagram with one directed edge used as canonical non-empty content. | unknown | naming-lies |
| jekyll_mermaid_prebuild/digest_calculator_spec.rb | 19 | returns consistent digest | Verifies determinism by calling `DigestCalculator.content_digest` twice with the same `flowchart LR` source and asserting equality of both outputs; SUT entry point is `.content_digest(content)` executed twice in sequence; assertion target is stable repeated value identity; fixture shape is a two-line diagram string with one edge and no external state to isolate pure hashing behavior. | unknown | — |
| jekyll_mermaid_prebuild/digest_calculator_spec.rb | 29 | produces different digests | Verifies that digest output changes when diagram content changes by hashing two distinct `graph TD` strings and asserting inequality; SUT entry point is `.content_digest` invoked with two different source payloads; assertion target is non-equality across outputs; fixture shape is paired one-edge diagrams with different node labels to exercise content sensitivity rather than formatting metadata. | unknown | — |
| jekyll_mermaid_prebuild/digest_calculator_spec.rb | 38 | returns a valid digest | Verifies empty-input handling by calling `DigestCalculator.content_digest("")` and asserting output remains a String of length 8; SUT entry point is `.content_digest` with zero-length content; assertion targets are type and fixed-width shape only; fixture shape is an empty string edge case used to confirm method does not error and still emits a short token-like value. | unknown | vacuous-assertion |
| jekyll_mermaid_prebuild/digest_calculator_spec.rb | 47 | produces different digests for different whitespace | Verifies whitespace sensitivity by hashing semantically similar diagrams that differ only in indentation and asserting digest inequality; SUT entry point is `.content_digest` called with baseline and indented variants; assertion target is non-equality to confirm raw content affects digest; fixture shape is two near-identical two-line flowchart strings differing only in leading spaces before the edge line. | unknown | — |
| jekyll_mermaid_prebuild/emoji_compensator_spec.rb | 10 | detects flowchart from first line | Verifies `EmojiCompensator.detect_diagram_type` recognizes a direct `flowchart LR` header on line one and returns `"flowchart"`; SUT entry point is `.detect_diagram_type(source)`; assertion target is exact type string equality; fixture shape is a compact two-line flowchart source with the type keyword at the start followed by one edge declaration. | unknown | — |
| jekyll_mermaid_prebuild/emoji_compensator_spec.rb | 16 | detects graph as flowchart alias | Verifies alias normalization by passing a `graph TD` declaration and expecting `"flowchart"` from `detect_diagram_type`; SUT entry point is `.detect_diagram_type(source)`; assertion target is canonical type mapping from alias to flowchart; fixture shape is two-line diagram text where the first line uses `graph` rather than `flowchart`. | unknown | — |
| jekyll_mermaid_prebuild/emoji_compensator_spec.rb | 22 | detects type after YAML frontmatter | Verifies parser skips YAML frontmatter and still finds diagram type line by expecting `"flowchart"` after a frontmatter block; SUT entry point is `.detect_diagram_type(source)`; assertion target is exact normalized type string; fixture shape is heredoc containing opening and closing `---` metadata block, then `flowchart LR`, then one edge line. | unknown | — |
| jekyll_mermaid_prebuild/emoji_compensator_spec.rb | 34 | detects type after comment lines | Verifies comment prelude handling by ensuring `detect_diagram_type` ignores leading `%%` comment lines and resolves `"flowchart"` from the subsequent header; SUT entry point is `.detect_diagram_type(source)`; assertion target is exact type value; fixture shape is heredoc with two mermaid comment lines before a `flowchart LR` declaration and one edge definition. | unknown | — |
| jekyll_mermaid_prebuild/emoji_compensator_spec.rb | 45 | detects type after frontmatter, comments, and blank lines | Verifies robust preamble skipping when YAML frontmatter, blank lines, and mermaid comments precede the diagram declaration, expecting `"flowchart"`; SUT entry point is `.detect_diagram_type(source)`; assertion target is canonical flowchart classification; fixture shape is mixed-structure heredoc combining metadata, whitespace, comments, then `flowchart LR` plus edge content. | unknown | — |
| jekyll_mermaid_prebuild/emoji_compensator_spec.rb | 60 | detects sequenceDiagram as different type | Verifies non-flowchart classification by passing a `sequenceDiagram` source and asserting the detected type is not `"flowchart"`; SUT entry point is `.detect_diagram_type(source)`; assertion target is negative equality guard against alias overreach; fixture shape is two-line sequence diagram snippet with one message arrow to represent a different diagram family. | unknown | — |
| jekyll_mermaid_prebuild/emoji_compensator_spec.rb | 66 | returns nil for empty or whitespace-only source | Verifies null classification for blank inputs by invoking `detect_diagram_type` on empty and whitespace-only strings and expecting `nil` both times; SUT entry point is `.detect_diagram_type(source)` with two edge-case payloads; assertion targets are nil results; fixture shape is one truly empty string and one multiline whitespace string to cover no-content normalization paths. | unknown | — |
| jekyll_mermaid_prebuild/emoji_compensator_spec.rb | 74 | appends two nbsp per emoji in flowchart label | Verifies baseline compensation by running `.compensate` on a flowchart label containing one emoji and asserting two trailing `&nbsp;` entities are inserted inside the label text; SUT entry point is `.compensate(source, "flowchart")`; assertion target is transformed bracket-label substring; fixture shape is single-edge flowchart with quoted node label `🔧 Code` and no existing padding. | unknown | — |
| jekyll_mermaid_prebuild/emoji_compensator_spec.rb | 81 | appends two nbsp per emoji when label has multiple emoji | Verifies scaling rule that compensation grows with emoji count by asserting a two-emoji label gains four `&nbsp;` entities after compensation; SUT entry point is `.compensate(source, "flowchart")`; assertion target is exact transformed label include with multiplied padding; fixture shape is simple flowchart where one node label contains two emoji tokens and no prior entities. | unknown | — |
| jekyll_mermaid_prebuild/emoji_compensator_spec.rb | 88 | returns source unchanged when label has no emoji | Verifies no-op behavior for non-emoji labels by asserting returned source exactly equals original flowchart text; SUT entry point is `.compensate(source, "flowchart")`; assertion target is full string equality unchanged; fixture shape is minimal `flowchart LR` input with plain ASCII node label to ensure compensation does not touch non-emoji content. | unknown | — |
| jekyll_mermaid_prebuild/emoji_compensator_spec.rb | 95 | adds compensation on top of existing nbsp in label | Verifies additive behavior when a label already contains `&nbsp;` entities by expecting two additional entities to be appended beyond existing padding; SUT entry point is `.compensate(source, "flowchart")`; assertion target is transformed label include with old plus new entities; fixture shape is one-node flowchart label containing one emoji and pre-seeded two `&nbsp;` entities. | unknown | — |
| jekyll_mermaid_prebuild/emoji_compensator_spec.rb | 102 | pads the emoji line when it is the longest line in a multi-line label | Verifies multiline targeting by confirming compensation applies to the emoji-bearing line when that line is the longest visual line in a `<br/>`-split label; SUT entry point is `.compensate(source, "flowchart")`; assertion target is include of modified first line with two `&nbsp;`; fixture shape is node label `🔧 Line1<br/>Line2` embedded in a one-edge flowchart. | unknown | — |
| jekyll_mermaid_prebuild/emoji_compensator_spec.rb | 109 | skips padding when the longest line has no emoji | Verifies guard behavior for multiline labels where the longest line has no emoji by asserting the source remains unchanged; SUT entry point is `.compensate(source, "flowchart")`; assertion target is exact output equality with input; fixture shape is label containing short emoji line and longer non-emoji line separated by `<br/>`, exercising longest-line selection logic. | unknown | — |
| jekyll_mermaid_prebuild/emoji_compensator_spec.rb | 116 | uses visual length (emoji counts as 2) to determine longest line | Verifies longest-line calculation uses visual width semantics where emoji count as width two, then applies corresponding padding to the selected line; SUT entry point is `.compensate(source, "flowchart")`; assertion target is include containing first line plus four `&nbsp;`; fixture shape is multiline label with `🔧🔧 A` versus `ABCDE` to force width-based tie-breaking behavior. | unknown | — |
| jekyll_mermaid_prebuild/emoji_compensator_spec.rb | 123 | pads only nodes whose labels contain emoji | Verifies selective transformation across multiple nodes by asserting emoji labels are padded while non-emoji label stays unchanged in the same diagram; SUT entry point is `.compensate(source, "flowchart")`; assertion targets are three includes covering padded first node, untouched middle node, padded final node; fixture shape is three-node heredoc flowchart with mixed emoji and plain labels. | unknown | — |
| jekyll_mermaid_prebuild/emoji_compensator_spec.rb | 136 | returns source unchanged when diagram type is not flowchart | Verifies type gate by passing `sequenceDiagram` type to `.compensate` and asserting no transformation occurs; SUT entry point is `.compensate(source, "sequenceDiagram")`; assertion target is full source equality; fixture shape is two-line sequence diagram text with emoji in participant label to ensure unchanged result is due to type, not missing emoji. | unknown | — |
| jekyll_mermaid_prebuild/emoji_compensator_spec.rb | 143 | preserves HTML entities in labels | Verifies compensation does not corrupt existing HTML entities by asserting output still contains `&amp;` while adding emoji padding in label; SUT entry point is `.compensate(source, "flowchart")`; assertion targets are presence of original entity and transformed label suffix; fixture shape is one-edge flowchart where node text includes both emoji and encoded ampersand entity. | unknown | — |
| jekyll_mermaid_prebuild/emoji_compensator_spec.rb | 151 | compensates all common flowchart node shapes | Verifies shape coverage by iterating through bracket, round, diamond, and double-circle node syntaxes and asserting each compensated result includes `&nbsp;`; SUT entry point is `.compensate(source, "flowchart")` inside shape loop; assertion target is per-shape inclusion of padding plus custom failure message; fixture shape is generated one-edge flowchart per shape variant with single emoji label and common base header. | unknown | — |
| jekyll_mermaid_prebuild/emoji_compensator_spec.rb | 167 | compensates when diagram uses graph keyword | Verifies alias compatibility in compensation path by using `graph LR` source while passing normalized flowchart type and asserting emoji padding appears; SUT entry point is `.compensate(source, "flowchart")`; assertion target is transformed label include with two entities; fixture shape is minimal alias-based graph declaration with one quoted emoji node and single edge. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 14 | stores configuration | Verifies object initialization preserves provided configuration double by asserting `generator.config` equals injected `config`; SUT entry point is `Generator.new(config)` and reader method `#config`; assertion target is object identity equality of stored config; fixture shape is instance_double of `Configuration` built from helper attributes including cache directory path rooted under temporary test workspace. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 29 | returns cached path without calling mmdc | Verifies cache hit behavior by prewriting `<cache_key>.svg`, asserting `MmdcWrapper.render` is not invoked, and expecting returned hash maps cache key to existing file path; SUT entry point is `generator.generate(source, key)`; assertion targets are no-render interaction plus exact path mapping; fixture shape is created cache directory containing one preexisting SVG file and simple mermaid source string. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 47 | calls mmdc and returns cache path | Verifies cache miss generation path by stubbing render to write SVG, invoking `generate`, and asserting returned path matches expected cache location and file exists; SUT entry point is `generator.generate(source, key)`; assertion targets are hash lookup path equality and filesystem existence check; fixture shape is empty cache directory with mmdc wrapper stub that writes `<svg>generated</svg>`. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 55 | writes the mmdc output to the cache file | Verifies generated file content persistence by stubbing render to write known SVG text, running `generate`, then reading resulting cache file and asserting exact content equality; SUT entry point is `generator.generate(source, key)` followed by filesystem read; assertion target is byte-for-byte equality to `<svg>generated</svg>`; fixture shape is empty cache with deterministic render stub output. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 68 | returns nil | Verifies failure propagation when `MmdcWrapper.render` returns false by asserting `generator.generate` returns `nil`; SUT entry point is `generator.generate(source, key)` under failing renderer stub; assertion target is nil result as failure signal; fixture shape is simple graph source with wrapper globally stubbed to fail and no preexisting cache artifact. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 96 | calls mmdc with dark theme | Verifies dark mode configuration forwarding by running `generate` under `prefers_color_scheme: :dark` and asserting wrapper received render call with `theme: :dark` and expected output path; SUT entry point is `generator.generate(source, key)` plus wrapper spy; assertion target is `have_received(:render).with(...)` argument contract; fixture shape is dark-config generator and render stub writing deterministic SVG with light background to be post-processed. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 106 | returns the cached path | Verifies dark-mode generation still returns base cache key path mapping by invoking `generate` and asserting hash value for key equals `<cache_dir>/<key>.svg`; SUT entry point is `generator.generate(source, key)` under dark config; assertion target is exact returned path string; fixture shape is writable cache directory and dark render stub that emits SVG content for post-processing. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 111 | applies configured dark chart background (default black) | Verifies dark output mutation by generating SVG under dark preference, reading file, asserting black background style appears, and white background style is removed; SUT entry point is `generator.generate(source, key)` followed by file read; assertion targets are positive and negative substring checks on background color tokens; fixture shape is initial SVG style string containing white background that should be transformed to black. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 140 | writes light and dark SVGs and returns both stems | Verifies auto color-scheme mode emits two variants by generating once and asserting returned hash keys exactly include base key and `-dark` key; SUT entry point is `generator.generate(source, key)` under auto config; assertion target is exact key set via `contain_exactly`; fixture shape is render stub writing same source SVG and cache directory initially empty for dual-output creation path. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 146 | applies light and dark chart backgrounds (default white / black) | Verifies auto mode post-processing by reading both returned SVG files and asserting light variant keeps white background while dark variant uses black and excludes white token; SUT entry point is `generator.generate(source, key)` in auto mode; assertion targets are substring presence and absence across two files; fixture shape is baseline SVG string with white background duplicated then transformed into separate light and dark outputs. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 162 | renders only the dark variant | Verifies partial-cache optimization in auto mode when light file exists but dark file does not by expecting exactly one render call and asserting returned map includes dark key with existing file; SUT entry point is `generator.generate(source, key)`; assertion targets are render call count, dark-key presence, and dark file existence; fixture shape is cache pre-seeded with light SVG only plus render stub enabled. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 176 | does not call mmdc | Verifies full-cache short circuit in auto mode by precreating both light and dark SVG files, asserting wrapper is not called, and invoking `generate`; SUT entry point is `generator.generate(source, key)` with both files present; assertion target is no-render interaction expectation; fixture shape is cache directory containing `<key>.svg` and `<key>-dark.svg` before call. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 194 | returns nil | Verifies failure handling when auto mode light render succeeds but dark render fails by stubbing theme-sensitive render behavior and asserting overall `generate` result is nil; SUT entry point is `generator.generate(source, key)` with conditional render stub; assertion target is nil return on partial generation failure; fixture shape is empty cache and stub that writes light file but returns false for dark theme. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 221 | widens edge label foreignObjects regardless of diagram type | Verifies edge label padding feature by generating from SVG containing `foreignObject width="40"` under positive `edge_label_padding` and asserting written output includes widened `width="45"`; SUT entry point is `padded_generator.generate(source, key, diagram_type: "flowchart")`; assertion target is width attribute change in emitted SVG; fixture shape is minimal SVG snippet with one `edgeLabel` block and deterministic render stub. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 243 | injects text-centering CSS into the SVG | Verifies default text-centering post-processing by generating fresh SVG and asserting resulting file contains `text-align:center` CSS; SUT entry point is `generator.generate(source, key)`; assertion target is inclusion of centering style token in output SVG; fixture shape is generated SVG containing style block and foreignObject element, enabling insertion point for additional CSS rules. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 248 | injects foreignObject overflow:visible CSS into the SVG | Verifies overflow-protection injection by generating SVG and asserting output includes `overflow:visible` styling for foreignObject content; SUT entry point is `generator.generate(source, key)`; assertion target is presence of overflow visibility CSS token; fixture shape is simple SVG with style and foreignObject tags supplied by render stub to trigger style augmentation path. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 275 | does not inject centering CSS | Verifies opt-out behavior when `text_centering: false` by generating SVG with a specialized generator and asserting output lacks `text-align:center`; SUT entry point is `no_centering_generator.generate(source, key)`; assertion target is negative substring check for centering CSS; fixture shape is same base SVG stub as centering tests but configuration disables centering transformation. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 302 | does not inject overflow CSS | Verifies overflow-protection opt-out when `overflow_protection: false` by generating SVG and asserting output does not include `overflow:visible`; SUT entry point is `no_overflow_generator.generate(source, key)`; assertion target is negative CSS token check; fixture shape is standard styled SVG with foreignObject, using config double that disables overflow mutation while leaving other processing intact. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 310 | builds URL with output_dir and cache_key | Verifies URL assembly helper by calling `build_svg_url("abc12345")` and asserting exact path `/assets/svg/abc12345.svg`; SUT entry point is `generator.build_svg_url(cache_key)`; assertion target is full string equality for generated public URL; fixture shape is generator configured with output directory attributes and fixed key literal to validate deterministic path composition. | unknown | — |
| jekyll_mermaid_prebuild/generator_spec.rb | 318 | generates figure with linked image | Verifies figure HTML builder returns markup containing figure wrapper, anchor link, image tag with provided URL in `href` and `src`, plus closing tags; SUT entry point is `generator.build_figure_html(light_url)`; assertion targets are substring includes for key markup fragments; fixture shape is single light URL string `/assets/svg/abc.svg` with no dark variant to exercise one-image branch. | unknown | presentation-coupled |
| jekyll_mermaid_prebuild/generator_spec.rb | 328 | emits two links and prefers-color-scheme CSS when dark_url is set | Verifies dark-variant HTML branch emits media-query CSS and both light and dark link-image elements when `dark_url` is provided; SUT entry point is `generator.build_figure_html(light_url, dark_url: ...)`; assertion targets are includes for media rule, class names, anchor tags, and dark image source; fixture shape is paired light and dark SVG URLs with deterministic string constants. | unknown | presentation-coupled |
| jekyll_mermaid_prebuild/mmdc_wrapper_spec.rb | 17 | returns true | Verifies availability check returns true when `command_exists?("mmdc")` is stubbed true; SUT entry point is `MmdcWrapper.available?`; assertion target is boolean true; fixture shape is isolated class-level stub for command presence with cache reset in `before` to prevent stale availability memoization across examples. | unknown | — |
| jekyll_mermaid_prebuild/mmdc_wrapper_spec.rb | 27 | returns false | Verifies availability check returns false when `command_exists?("mmdc")` is stubbed false; SUT entry point is `MmdcWrapper.available?`; assertion target is boolean false; fixture shape is explicit negative command-existence stub combined with per-example cache reset to isolate behavior from prior availability checks. | unknown | — |
| jekyll_mermaid_prebuild/mmdc_wrapper_spec.rb | 40 | returns true when executable exists in PATH | Verifies low-level command discovery by creating a temporary executable on PATH and asserting `command_exists?("testcmd")` returns true; SUT entry point is `MmdcWrapper.command_exists?(cmd)`; assertion target is boolean true lookup result; fixture shape is tmpdir PATH override with platform-aware executable filename and executable permission set for non-Windows systems. | unknown | — |
| jekyll_mermaid_prebuild/mmdc_wrapper_spec.rb | 52 | returns false when executable does not exist | Verifies command discovery negative path by setting PATH to nonexistent directory and asserting `command_exists?("nonexistent_cmd")` is false; SUT entry point is `MmdcWrapper.command_exists?(cmd)`; assertion target is boolean false; fixture shape is controlled PATH environment override restored by `around` hook to avoid leak between examples. | unknown | — |
| jekyll_mermaid_prebuild/mmdc_wrapper_spec.rb | 66 | returns trimmed version string | Verifies version extraction trims trailing newline when `Open3.capture2e("mmdc", "--version")` succeeds with output `11.12.0\n`; SUT entry point is `MmdcWrapper.version`; assertion target is exact string `"11.12.0"`; fixture shape is Open3 stub returning success status double and version stdout including newline terminator. | unknown | — |
| jekyll_mermaid_prebuild/mmdc_wrapper_spec.rb | 78 | returns nil | Verifies version lookup failure handling by stubbing `capture2e` to return unsuccessful status and asserting `MmdcWrapper.version` returns nil; SUT entry point is `.version`; assertion target is nil value on failed command status; fixture shape is Open3 failure stub with empty output and process-status double reporting `success?` false. | unknown | — |
| jekyll_mermaid_prebuild/mmdc_wrapper_spec.rb | 90 | returns :not_found | Verifies status probe reports `:not_found` when `mmdc` command is absent by stubbing `command_exists?` false and asserting check result; SUT entry point is `MmdcWrapper.check_status`; assertion target is symbol equality to `:not_found`; fixture shape is class-level command existence stub without invoking render path due early availability guard. | unknown | — |
| jekyll_mermaid_prebuild/mmdc_wrapper_spec.rb | 101 | returns :puppeteer_error | Verifies status probe maps render self-test failure by stubbing command existence true and `test_render` to `:puppeteer_error`, then asserting check status mirrors that symbol; SUT entry point is `MmdcWrapper.check_status`; assertion target is symbol equality to `:puppeteer_error`; fixture shape is two-layer class stub controlling both availability and render-probe outcome. | unknown | — |
| jekyll_mermaid_prebuild/mmdc_wrapper_spec.rb | 112 | returns :ok | Verifies happy status path by stubbing command existence true and `test_render` to `:ok`, then asserting `check_status` returns `:ok`; SUT entry point is `MmdcWrapper.check_status`; assertion target is success status symbol equality; fixture shape is class stubs forcing available command and successful test render result with no filesystem dependency. | unknown | — |
| jekyll_mermaid_prebuild/mmdc_wrapper_spec.rb | 121 | invokes mmdc without -t for default theme | Verifies command construction for default theme by expecting `Open3.capture3` called with `mmdc -i <tmp>.mmd -o /tmp/out.svg -e svg` and no theme flag; SUT entry point is `MmdcWrapper.render(source, out, theme: :default)`; assertion target is exact subprocess argument list; fixture shape is process status success double and matcher for generated temporary input file ending in `.mmd`. | unknown | — |
| jekyll_mermaid_prebuild/mmdc_wrapper_spec.rb | 129 | appends -t dark when theme is :dark | Verifies dark-theme command construction adds `-t dark` to mmdc invocation by expecting exact `Open3.capture3` argument sequence including theme pair; SUT entry point is `MmdcWrapper.render(source, out, theme: :dark)`; assertion target is subprocess argument list with dark flag; fixture shape is success status double and temporary input path matcher, identical baseline to default-theme case with one option difference. | unknown | — |
| jekyll_mermaid_prebuild/mmdc_wrapper_spec.rb | 137 | returns false when mmdc fails | Verifies render failure path returns false when subprocess status reports unsuccessful execution; SUT entry point is `MmdcWrapper.render("x", "/tmp/nope.svg")`; assertion target is boolean false result; fixture shape is Open3 stub returning stderr `"err"` and process status double with `success?` false to mimic mmdc runtime failure. | unknown | — |
| jekyll_mermaid_prebuild/mmdc_wrapper_spec.rb | 144 | raises ArgumentError for an unsupported theme | Verifies theme validation rejects unsupported values by calling render with `theme: :forest` and asserting `ArgumentError` with message matching `unsupported mmdc theme`; SUT entry point is `MmdcWrapper.render(source, out, theme: invalid)`; assertion target is exception class and message regex; fixture shape is simple graph source and output path literals, with no subprocess call expected for preflight argument guard. | unknown | — |
| jekyll_mermaid_prebuild/mmdc_wrapper_spec.rb | 154 | matches backtick fenced mermaid blocks | Verifies fence regex matches triple-backtick mermaid block and captures diagram body in group 2 for backtick fences; SUT entry point is `MmdcWrapper.mermaid_fence_pattern` used with `String#match`; assertion targets are non-nil match object and exact captured inner content; fixture shape is inline markdown code fence string with `mermaid` language and two-line diagram payload. | unknown | — |
| jekyll_mermaid_prebuild/mmdc_wrapper_spec.rb | 162 | matches tilde fenced mermaid blocks | Verifies same fence regex supports tilde fences by matching `~~~mermaid` block and asserting capture group 2 equals embedded flowchart text; SUT entry point is `MmdcWrapper.mermaid_fence_pattern` plus string match; assertion targets are match presence and exact captured diagram block; fixture shape is markdown snippet using tilde delimiters and two-line flowchart content. | unknown | — |
| jekyll_mermaid_prebuild/mmdc_wrapper_spec.rb | 170 | matches 4+ fence characters | Verifies regex handles fences longer than three markers by matching four-backtick mermaid block and checking captured content; SUT entry point is `MmdcWrapper.mermaid_fence_pattern`; assertion targets are non-nil match and exact diagram capture; fixture shape is markdown fence string using four backticks on open and close with single-edge graph body. | unknown | — |
| jekyll_mermaid_prebuild/mmdc_wrapper_spec.rb | 178 | does not match mismatched fence types | Verifies regex rejects mixed delimiter closures by using opening backticks and closing tildes and asserting match is nil; SUT entry point is `MmdcWrapper.mermaid_fence_pattern` with `String#match`; assertion target is nil match for invalid fence pair; fixture shape is malformed markdown snippet with mermaid header and mismatched close fence characters. | unknown | — |
| jekyll_mermaid_prebuild/mmdc_wrapper_spec.rb | 185 | does not match non-mermaid code blocks | Verifies regex does not capture code fences for other languages by matching against a `ruby` fenced block and asserting nil; SUT entry point is `MmdcWrapper.mermaid_fence_pattern`; assertion target is absence of match for non-mermaid language tag; fixture shape is short ruby code fence content using standard triple backticks. | unknown | — |
| jekyll_mermaid_prebuild/mmdc_wrapper_spec.rb | 192 | captures the fence characters | Verifies capture group 1 records the opening fence token itself by matching a simple mermaid block and asserting first group equals `` ``` ``; SUT entry point is `MmdcWrapper.mermaid_fence_pattern` with match extraction; assertion target is exact capture-group equality for delimiter characters; fixture shape is minimal triple-backtick mermaid fence containing one-line graph body and matching close fence. | unknown | — |
